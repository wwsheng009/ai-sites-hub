# 需求分析 — AI 中转站点集中管理平台（ai-sites-client）

> 版本：v0.2（2026-09-19）
> 状态：**需求已确认**（2026-09-19）：① 单管理员；② 通知模块立项，设计参考 ai-gateway 通知子系统（见 FR-9）；③ 渠道管理暂缓
> 参考项目分析：见 `../analysis/`（sub2api / new-api / ai-gateway）

## 1. 背景

当前用户使用大量 AI 中转站点（主要为 **sub2api** 与 **new-api** 两类系统部署），散落在各处，管理成本高：

- 每个站点有独立的账号、登录态、key/token，需要逐站登录查看
- 部分站点有**签到送额度**机制（new-api 类），忘记签到即损失额度
- key 列表、分组、额度信息无法集中对比与检索
- 缺少统一的命令行/界面操作入口

已有部分实践：`E:\projects\ai\ai-gateway` 内置的 AI Sites 子系统验证了「站点目录 + 自动识别 + 凭据登录 + 监控同步 + 签到」的可行性，但它是大型网关的一个子模块，不适合独立使用。

## 2. 项目定位与目标

**做一个独立的、轻量的 AI 中转站点集中管理平台**：

- 只做「站点管理与信息聚合」，**不做模型中转代理**（与 ai-gateway 的区别）
- 三种操作入口：**Web 前端 / 服务端 HTTP API / cmd 命令行**
- 上游站点类型：sub2api、new-api，可扩展

### 目标（Goals）

| # | 目标 | 衡量方式 |
|---|------|----------|
| G1 | 登记一个站点 URL 后，自动判断它是 sub2api 还是 new-api | 识别准确率 ≥ 两类站点的特征打分（参考 ai-gateway 方案） |
| G2 | 存入账号密码后，系统可自动登录并维持登录态 | 定时作业周期内登录态不失效；失效可自动重登 |
| G3 | new-api 类站点自动定时签到 | 签到作业执行后站点额度增加（或明确报告不可签到的原因） |
| G4 | 集中查询所有站点的 key 列表 / 分组 / 额度 | 一条命令 / 一个页面看到全部站点的 key 与额度汇总 |
| G5 | 服务端 / 前端 / cmd 三入口能力一致 | 三入口共用同一 service 层 |

### 非目标（Non-goals）

- 不做 LLM 请求代理/转发（不实现 /v1/chat/completions 之类的透传）
- 不做站点部署管理（不负责安装/升级 sub2api、new-api 本身）
- 不做多用户 SaaS；第一版单管理员使用
- 不做浏览器自动化（playwright 等）；仅 HTTP API 对接

## 3. 用户与角色

| 角色 | 说明 |
|------|------|
| 管理员（唯一/少量） | 登记站点、配置凭据、管理作业、查看全部数据 |
| cmd 操作者 | 与管理员同一人，通过命令行执行相同操作 |
| （预留）只读观察者 | 只看汇总数据，不能改配置（二期） |

## 4. 术语表

| 术语 | 含义 |
|------|------|
| 站点（Site） | 一个部署的 sub2api 或 new-api 实例，用 base URL 标识 |
| 站点类型（site_type） | `sub2api` / `new-api` / `unknown` |
| 凭据（Credential） | 登录站点所需的 secrets：账号密码 / access token / refresh token / PAT |
| Key / Token | 站点内签发的 sk- 或 API key（sub2api 的 key，new-api 的 token） |
| 分组（Group） | 站点内的 key 分组/可用分组及倍率 |
| 签到（Check-in） | new-api 类站点的每日签到领额度行为 |
| Adapter | 站点类型适配器，把不同站点 API 收敛为统一内部接口 |

## 5. 功能需求（FR）

### FR-1 站点登记与自动识别

- FR-1.1 支持录入站点：`name`、`base_url`（或仅 URL，系统推断 API 地址）、备注
- FR-1.2 **自动识别站点类型**：抓取站点公开端点（new-api：`GET /api/status`；sub2api：`GET /setup/status`、`GET /api/v1/settings/public`）+ 落地页特征打分，输出 `site_type` 与置信度（参考 ai-gateway `site_detect.go` 的 score≥6 方案）
- FR-1.3 识别结果可人工覆盖（`site_type` 手动指定）
- FR-1.4 站点生命周期状态：`active` / `disabled` / `archived`

### FR-2 凭据管理与自动登录

- FR-2.1 每站点可配置凭据，支持三种模式（对齐 ai-gateway）：
  - `username_password`：账号+密码（可含 TOTP secret）
  - `token`：直接给 access token / PAT（new-api PAT、sub2api JWT 对）
  - `hybrid`：token 优先，失效回退密码重登
- FR-2.2 **自动登录**：凭据校验通过后立即登录验证；定时作业维持登录态
- FR-2.3 凭据阶梯：new-api：PAT → 密码重登；sub2api：access token → refresh token → 密码重登
- FR-2.4 认证状态机显式记录：`ok / login_failed / totp_required / turnstile_blocked / cf_blocked / token_expired / refresh_failed`，前端与 cmd 可读
- FR-2.5 凭据静态加密存储（AES-GCM），任何日志/快照/API 响应不得出现明文凭据

### FR-3 定时签到（后台作业）

- FR-3.1 new-api 站点：定时执行 `GET /api/user/checkin`（查询 enabled/状态）→ `POST /api/user/checkin`（签到），记录 `quota_awarded` 与签到日期
- FR-3.2 站点开启 Turnstile/验证码导致无法自动签到时：状态记为 `blocked_by_turnstile`，进入待人工清单，不反复重试
- FR-3.3 sub2api 站点：签到能力标记 `unsupported`，作业跳过并说明
- FR-3.4 签到调度可配置：默认每日一次（随机分钟抖动防集中），可按站点覆盖
- FR-3.5 签到历史可查询：按站点/按月，含成功/失败/跳过原因

### FR-4 Key / 分组 / 额度集中查询

- FR-4.1 定时（及手动触发）同步每站点的 key 列表：
  - sub2api：`GET /api/v1/keys`（分页）+ `GET /api/v1/groups/available`
  - new-api：`GET /api/token/`（分页，打码）→ 需要明文时 `POST /api/token/:id/key`（可配置是否抓明文）
- FR-4.2 key 归一化字段（统一投影，参考 ai-gateway quota projection）：
  `site_id, remote_key_id, name, group, status, limit/used/remaining（额度三元组）, unlimited, expires_at, created_at, key_source（打码/明文）`
- FR-4.3 分组与倍率：sub2api `groups/available` + `groups/rates`；new-api `GET /api/user/self/groups` + `/api/ratio_config`
- FR-4.4 原始响应存 snapshot（可回放/排查），展示用归一化投影 + `freshness`（fresh/stale/missing）标记
- FR-4.5 全局视图：跨站点 key 汇总列表（筛选：站点/分组/状态/即将过期/低余额）、站点额度汇总卡片

### FR-5 站点健康与观测

- FR-5.1 站点连通性探测：定时 ping 公开端点，记录可用性与延迟
- FR-5.2 登录态/token 有效期监控，即将过期预警
- FR-5.3 事件流水：站点类型变更、登录失败、签到成功/失败、额度异动等，作为前端通知与 cmd `events` 数据源

### FR-6 服务端 HTTP API（本平台自身的 API）

- FR-6.1 REST 风格，统一响应信封；按域分组路由（参考 sub2api routes 注册模式）
- FR-6.2 资源域：`/sites`（CRUD+识别）、`/sites/:id/credentials`（写入+状态）、`/sites/:id/sync`（手动触发同步）、`/keys`（跨站汇总）、`/groups`、`/checkins`（历史+手动签到）、`/jobs`（作业状态/触发）、`/events`
- FR-6.3 平台自身认证：管理员账号+JWT（首版单管理员），预留 PAT 供脚本调用
- FR-6.4 敏感字段（凭据、明文 key）读取需显式参数 `?reveal=true` 且默认打码

### FR-7 cmd 命令行管理

- FR-7.1 单二进制多子命令（cobra），与 HTTP API 共用 service 层
- FR-7.2 命令集（首版）：
  - `sites list|add|detect|enable|disable|remove|import|export`
  - `auth login <site> | status [--site]`
  - `checkin run [--site ALL] | history [--site --month]`
  - `keys list [--site --group --status --expiring --low-quota] | reveal <site> <key-id>`
  - `groups list [--site]`
  - `sync run [--site] [--full]`
  - `jobs list|run <job>|status`
  - `serve`（启动 HTTP 服务+后台作业）
  - `doctor`（体检：配置/DB/各站点连通性/凭据状态）
- FR-7.3 `import/export`：站点与凭据可批量导入导出（JSON/YAML，凭据导出需显式 `--with-secrets` + 确认）

### FR-8 Web 前端

- FR-8.1 直接复用 `E:\projects\ai\sub2api` 前端骨架（Vue3 + TS + Vite + Pinia + Tailwind，无组件库），仅取骨架/样式/布局/基建，不迁移其业务后端功能
- FR-8.2 页面（首版）：
  - 登录页（平台自身）
  - **站点管理页**：列表（类型徽标/健康/登录态/最近同步）+ 新增向导（输入 URL → 自动识别 → 填凭据 → 测试登录）
  - **站点详情页**：凭据状态、key 列表、分组倍率、签到历史、事件流水、手动同步/签到按钮
  - **全局 Keys 页**：跨站 key 汇总表格（筛选/排序/打码显示，reveal 需确认）
  - **作业页**：作业列表、最近运行、手动触发、失败原因
  - **总览仪表盘**：站点数/签到概况/额度汇总/异常清单
- FR-8.3 API 层对接本平台 API；样式沿用 sub2api 骨架的 Tailwind 方案与暗色模式

### FR-9 通知模块（2026-09-19 立项，设计参考 ai-gateway 通知子系统）

- FR-9.1 事件接入：FR-5.3 的事件流水作为通知事件源；可通知的事件类别至少包括：签到结果（成功/失败/被 Turnstile 拦截）、登录失败/token 失效、站点不可达、额度异动/低余额
- FR-9.2 通知策略：按「事件类别 × 级别 × 站点」定义规则，绑定目标渠道；支持启用/禁用与静默窗口（避免重复轰炸）
- FR-9.3 通知渠道：一期内置 **webhook**（通用 JSON POST）与 **email（SMTP）**；渠道配置支持测试发送；其余渠道（Telegram/Bark/钉钉/飞书等）参考 ai-gateway 的渠道抽象按需扩展
- FR-9.4 投递可靠性：异步投递 + 重试 + 投递记录（状态/失败原因/重试次数）可查询；投递内容不含明文凭据/明文 key（NFR-3）
- FR-9.5 管理：渠道与策略的 CRUD（HTTP API + 前端设置页 + cmd）；事件→策略→投递的匹配过程可追溯（便于排查"为什么没收到通知"）

## 6. 非功能需求（NFR）

| # | 需求 | 说明 |
|---|------|------|
| NFR-1 | 技术栈 | 后端 Go（单二进制，嵌入前端产物）；前端 Vue3+Vite+Tailwind（复用 sub2api 骨架） |
| NFR-2 | 存储 | 首版 SQLite（单文件、零运维）；schema 用迁移文件管理；**不**同时维护双数据库方言（吸取 ai-gateway 教训） |
| NFR-3 | 安全 | 凭据 AES-GCM 加密落库；主加密密钥来自配置/环境变量；日志与快照脱敏；明文 key 默认不抓取，抓取需显式开启 |
| NFR-4 | 对上游友好 | 定时作业随机抖动 + 全局限速 + 429 退避；探测/同步预算（参考 ai-gateway probe 预算）；响应体大小上限 |
| NFR-5 | 作业可靠性 | 作业执行记录持久化；失败有原因分类；支持手动重跑；进程重启后作业状态可恢复 |
| NFR-6 | 可移植 | Windows/Linux 可运行；配置文件 YAML + 环境变量覆盖；`doctor` 一键体检 |
| NFR-7 | 代码卫生 | 仓库不放二进制/日志/pid 产物；单一 cron/调度实现；文档与代码同仓维护 |

## 7. 范围与依赖边界

### 上游站点能力边界（依据 analysis/ 三份文档）

| 能力 | sub2api | new-api |
| ---- | ------- | ------- |
| 密码登录 | ✅ `/api/v1/auth/login`（JWT 对） | ✅ `/api/user/login`（15min JWT + refresh cookie） |
| 无人值守 token | ✅ access/refresh token 对 | ✅ PAT（推荐）/ 15min JWT |
| Key 列表 | ✅ `/api/v1/keys` | ✅ `/api/token/`（打码；明文需另调） |
| 分组/倍率 | ✅ `groups/available`、`groups/rates` | ✅ `/api/user/self/groups`、`/api/ratio_config` |
| 额度 | ✅ key 对象内 quota + `/user/platform-quotas` | ✅ `/api/usage/token/`（sk- 令牌） |
| **签到** | ❌ 无此接口 → `unsupported` | ✅ `GET/POST /api/user/checkin`（Turnstile 时受限） |
| 公开识别端点 | ✅ `/setup/status`、`/api/v1/settings/public` | ✅ `/api/status` |

### 外部依赖

- 参考项目仅作**只读参考与骨架复制来源**，不引入其代码依赖、不修改它们
- 前端骨架复制自 sub2api `frontend/`（保留其 LICENSE/版权声明若适用）

## 8. 风险与开放问题

| # | 风险/问题 | 处理 |
|---|-----------|------|
| R1 | sub2api/new-api 版本差异导致 API 字段漂移 | adapter 内做字段容错 + snapshot 留证；识别/同步失败降级为 stale 而非报错 |
| R2 | Turnstile/CF 拦截自动化登录与签到 | 显式状态机 `turnstile_blocked/cf_blocked` + 待人工清单，不硬重试 |
| R3 | new-api 敏感操作（PAT 生成）需二次安全验证，无法全自动 | 首次接入引导流程：人工登录生成 PAT 录入平台（文档引导），此后全自动 |
| R4 | 站点限流（登录 20/min 等）触发封禁 | 全局限速 + 抖动 + 429 退避；作业默认 dry_run/低频 |
| R5 | 明文 key 的安全性 | 默认只存打码 key；reveal 单条审计；导出需显式参数 |
| Q1 | 是否需要多平台管理员/多用户？ | ✅ **已确认（2026-09-19）：只用单管理员**；多用户/角色保留为二期扩展 |
| Q2 | 签到额度变动是否需要通知（webhook/邮件）？ | ✅ **已确认（2026-09-19）：需要**，通知模块立项——事件→策略→渠道的设计参考 ai-gateway 通知子系统（专项分析见 `../analysis/ai-gateway-notification.md`，设计见 `../architecture/architecture.md` §11，FR-9） |
| Q3 | 是否需要管理站点内渠道（new-api /api/channel）？ | ⏸ **暂缓（2026-09-19）**：一期不做、不进里程碑；仅在 adapter 接口保留扩展位，未来需要时按独立 adapter 能力位追加 |

## 9. 里程碑概览（详细见 ../plan/implementation-plan.md）

- **M1 需求与设计冻结**：本文档（v0.2 已确认） + architecture 评审通过
- **M2 服务端骨架**：Go 工程搭建、配置、SQLite 迁移、site CRUD + 自动识别、`doctor`
- **M3 适配器与凭据**：sub2api/new-api adapter、凭据加密、自动登录、认证状态机
- **M4 同步与签到作业**：后台作业框架、key/分组/额度同步、签到作业、快照/投影
- **M4.5 通知模块**：事件→策略→渠道→投递（webhook + SMTP），设计参考 ai-gateway 通知子系统
- **M5 cmd 完整化**：FR-7 全部命令
- **M6 前端**：sub2api 骨架落地 + 六个页面
- **M7 打磨**：doctor/doctor e2e、导入导出、文档收尾
